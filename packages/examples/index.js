/**
 * List of node samples to experiment with basic node 
 * APIs and features
 */
const Logger = require('./module-patterns');
const logger = new Logger('default');
logger.log('This is a new log');

 